This is my dwm folder, which you will want to call in your .xinitrc

The bar requires python 3.6+.

By default, the autostart calls for pywal to set a wallpaper (this must be changed), and starts picom, sxhkd & the bar
